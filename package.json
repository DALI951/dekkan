{
  "name": "dekkan",
  "version": "0.1.0",
  "description": "Dekkan — Arabic-first small-shop kit. Cash box, stock, debts, profit. (The algorithm, tested.)",
  "private": true,
  "scripts": {
    "test": "node --test",
    "test:sim": "node scripts/simulate.js",
    "test:i18n": "node scripts/check-i18n.js",
    "check": "node --test && node scripts/simulate.js && node scripts/check-i18n.js"
  }
}